18:  Prolog Program: Database with Name and Date of Birth

% Base case: the sum from 1 to 1 is 1.
sum(1, 1).

% Recursive case: sum(N, Total)
sum(N, Total) :-
    N > 1,
    N1 is N - 1,
    sum(N1, SubTotal),
    Total is SubTotal + N.
